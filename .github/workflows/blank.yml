name: CI

on: [push]
  
jobs:
  run-some-shell-commands:
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: echo "Hello World"

      - name: Some Info
        run: |
          node -v
          npm -v
